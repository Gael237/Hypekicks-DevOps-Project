server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;

  location / {
    try_files $uri /index.html;
  }

  # Proxy API through frontend service (optional)
  location /api/auth/ {
  proxy_pass http://finlab-auth:3001/;
}
}
