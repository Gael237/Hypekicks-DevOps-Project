apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-auth
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Name }}-auth
  ports:
    - port: 3001
      targetPort: {{ .Values.auth.port }}
  type: ClusterIP